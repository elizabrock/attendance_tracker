%h1 Attendance Leaderboard
- if @students.empty?
  %p There are currently no students in the system

- else
  %table#leader_board
    %thead
      %tr
        %th Name
        %th Attendance
    %tbody
      - @students.each do |student|
        %tr
          %td= student.name
          %td #{student.attendances.percent_present}%
